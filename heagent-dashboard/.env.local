# Heagent Dashboard — Environment Variables
# Copy your vault .env values here (server-side only — never exposed to browser)

# Vault root path (absolute)
VAULT_PATH=/mnt/c/h-0/ai_employee_vault_bronze

# Odoo connection (server-side only)
ODOO_URL=http://172.31.192.1:8069
ODOO_DB=db_name
ODOO_USER=ubaidkamal420@gmail.com
ODOO_PASSWORD=my_password

# PM2 (for health check — pm2 jlist JSON output)
PM2_AVAILABLE=true
